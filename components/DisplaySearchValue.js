// components/DisplaySearchValue.js
import React from "react";

const DisplaySearchValue = ({ searchValue }) => {
  return (
    <div>
      <p>Search Value: {searchValue}</p>
      {/* Add more components or logic to display the search value */}
    </div>
  );
};

export default DisplaySearchValue;
